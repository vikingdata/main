

Note before install:

docker run -dit --restart unless-stopped cassandra1
docker run -dit --restart unless-stopped cassandra2
docker run -dit --restart unless-stopped cassandra3

docker run -dit --restart unless-stopped mongo1
docker run -dit --restart unless-stopped mongo2
docker run -dit --restart unless-stopped mongo3

docker run -dit --restart unless-stopped mysql1

--------------------------
on /etc/docker/daemon.json
   {
     "live-restore": true
   }

